// ── Talk Plugin Types ──
//
// Adds the `dialogue` character trait (scene id as dungeon.room.scene) and a
// Talk button in the character-sheet-top slot that opens that scene.
//
// No services or emitters are exposed; the engine's global `Character` type is
// used directly in the component.

export {};
